from product import Product, Products
from menu import Menu
import pickle

menu = Menu()
menu.start()

"""prod = Product(1, "lol", 12)
prodts = list()
prodts.append(prod)
prodts.append(Product(1, "kek", 10))

limit = [item for item in prodts if item.id == 1]
if limit:
    print(limit[0], limit[1])
else:
    print("kek")
"""