<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%=user.nickname%> Page</title>

        <link rel="stylesheet" href="/stylesheets/object.css">
    </head>
    <body>
        <a href="/"><img class="back" src="/images/back.png" alt="Back"/></a>
        <h2>Welcome, <%=user.nickname%>!</h2>
        <div class="body">

            <div class="buttons">
                <form action="/users/update">
                    <button class="button update">Update</button>
                </form>
                <form action="/users/delete" method="post">
                    <button class="button delete">Delete acc</button>
                </form>
                <br><br>
                <% if(user.statusAdmin){%>
                    <form action="/films/newfilm" method="get">
                        <button class="button subscribe">Add film</button>
                    </form>
                    <form action="/actors/newactor" method="get">
                        <button class="button subscribe">Add actor</button>
                    </form>
                <%}%>
            </div>
            <div class="userimage-text">
                <%if(user.avatar) {%>
                    <img class="mainuserimage" src="data:image/jpg;base64,<%=user.avatar.toString('base64')%>"/>
                <%} else {%>
                    <img class="mainuserimage" src="/images/nophoto.jpg"/>
                <%}%>
                <dl class="aboutuser">
                    <dt>Name: <%=user.name%></dt>
                    <dt>Nickname: <%=user.nickname%></dt>
                    <dt>Birth date: <%=user.birthdate%></dt>
                    <dt>Reg. date: <%=user.regdate%></dt>
                    <dt>Subscribed films:</dt><dd>
                    <% for (var i = 0; i < user.subfilms.length; i++) {%>
                        <a href="/films/<%=user.subfilms[i]._id%>"><%=(user.subfilms[i].title) %></a><%=(i==user.subfilms.length-1 ? ' ' : ' , ')%>
                    <%}%></dd>
                </dl>
            </div>
        </div>
    </body>
</html>
