<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%=user.nickname%> update</title>

        <link rel="stylesheet" href="/stylesheets/login.css">
    </head>
    <body>
        <a href="/users/userpage/<%=user._id%>"><img class="back" src="/images/back.png" alt="Back"/></a>
        <h2>Update <%=user.nickname%></h2>
        <form class="form" action="/users/update" method="post" enctype="multipart/form-data">
            <div class="imgcontainer">
                <%if(user.avatar) {%>
                    <img class="avatar-update" src="data:image/jpg;base64,<%=user.avatar.toString('base64')%>"/>
                <%} else {%>
                    <img class="avatar-update" src="/images/nophoto.jpg"/>
                <%}%>
            </div>
            <div class="container">
                <label>*leave fields empty if you dont want to change them</label><p></p>
                <div class="left"
                    <label><b>NEW name</b></label>
                    <input type="text" placeholder="<%=user.name%>" name="username">
                </div>
                <div class="right">
                    <label><b>NEW nickname</b></label>
                    <input type="text" placeholder="<%=user.nickname%>" name="usernickname" >
                </div>
                <div class="left">
                    <label><b>Birthdate</b></label>
                    <input type="date" name="birthdate">
                </div>
                <div class="right">
                    <label><b>Avatar</b></label>
                    <input type='file' name='avatarka'>
                </div>
                <label><b>NEW password</b></label>
                <input type="password" placeholder="NEW Password" name="password1" >
                <label><b>Repeat Password</b></label>
                <input type="password" placeholder="Repeat Password" name="password2" >
                <button type="submit">Update info</button>
            </div>
        </form>
    </body>
</html>

<!--

-->
